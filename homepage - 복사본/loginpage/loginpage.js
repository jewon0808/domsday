function loginAction(){
    const a = document.getElementById("username");
    const b = document.getElementById("passwold");

    // 임의의 값을 넣어둔 것임, 서버의 값을 변수로 선언해서 변경할 예정
    if(a==ppsalho&&b==pp0316){
        window.location.href = "https://www.naver.com";
    }
}